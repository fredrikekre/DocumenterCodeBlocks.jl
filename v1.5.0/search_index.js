var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#references","page":"Reference links & tooltips","text":"Identifiers in code blocks that name a documented object become links to the docstring — the code-block equivalent of Documenter's @ref. Resolution uses the same machinery and the page's CurrentModule meta, so a code block and an @ref link always agree on the target. CurrentModule applies positionally, exactly as it does for @ref: a page can switch modules with a second @meta block halfway down, and each code block resolves in the module in effect at its own position. Code blocks inside a docstring resolve in the docstring's own module, wherever the docstring is spliced.\n\nfunction demo(m::MyType)\n    t = MyType(3)\n    s = add_numbers(t.x, m.x)\n    greet()\n    return undocumented_helper(s)\nend\n\nHover (or click) MyType, add_numbers, and greet above — and note that undocumented_helper stays plain text: names that don't resolve are left alone, silently.\n\nLinks attach to call callees (add_numbers(...)), type positions (m::MyType), and plain value mentions — a documented name used as a function argument, on the right of =, in a condition, and so on, like zero(MyType) or a documented constant. What never links is a binding: a name to the left of =, a function parameter, a loop variable — there the name is not a use of the documented object (in the block above, the parameter m binds and stays plain):\n\na = foo(1)      # links the foo(a) docstring\nb = foo(1, 2)   # links the foo(a, b) docstring\nc = foo         # value mention → links, listing both methods\nd = DocumenterCodeBlocks.foo(1)   # qualified call\nfoo = c         # binding (left of =) → no link\n\nAs the block above shows, resolution is arity-aware: a call with n positional arguments links to the method documented with n arguments, not just to the first documented method. Qualified names resolve as a whole, but the link attaches to the name itself — the module qualifier and dot stay outside.\n\nMacro calls link on their name. No syntactic vouching is needed there: unlike a bare identifier, a macro name can only ever mean the macro.\n\n@twice greet()                       # unqualified macro name\nDocumenterCodeBlocks.@twice greet()  # qualified name\ns = w\"hello\"                         # string macro\n\nCode blocks inside docstrings get reference links too, with one exception: a reference with the enclosing docstring among its candidate targets is left unlinked — it (possibly) means the very thing the reader is looking at. A call whose arity resolves to a different documented method still links, so an example in foo(a, b)'s docstring that calls foo(1) links to foo(a). The signature header — a docstring's leading code block — links its argument and return types the same way (see clone's header in the API reference, which links MyType twice), while the documented name and the parameters stay plain.","title":"Reference links"},{"category":"section","location":"references/#tooltips","page":"Reference links & tooltips","text":"Every reference link has a doxygen-style hover tooltip showing the target's signature and the first sentence of its docstring. Tooltips are embedded in the page at build time and work offline.\n\nWhen a reference is ambiguous — several documented methods match — the tooltip lists all candidate signatures instead; hovering an entry previews its documentation and clicking navigates to it:\n\nfoo(args...)       # splat: positional count unknown → both methods listed\nq = qux(1)         # same arity as qux(\"…\") → both typed signatures listed\ny = add_numbers(1, 2)  # single documented method → plain tooltip","title":"Hover tooltips"},{"category":"section","location":"references/#Arity-pruning","page":"Reference links & tooltips","text":"Candidate lists only show methods that can actually take the call's argument count. measure has six documented methods:\n\nm = measure(args...)               # pure splat → all six methods listed\nml = measure(1, args...)           # at least 1 argument → the 0-argument method drops out\nm1 = measure(1)                    # arity 1 → only the three 1-argument methods\nm2 = measure(1, 2)                 # untyped (x, y) → exact match, single tooltip","title":"Arity pruning"},{"category":"section","location":"references/#Aggregated-docstrings","page":"Reference links & tooltips","text":"A bare @docs entry aggregates all of a function's docstrings under one anchor. Tooltips are arity-matched within the aggregate: a call site with a known argument count shows only the matching method's signature and summary, while an unknown count shows all signature headers:\n\nc2 = combine(1, 2)      # arity 2 → only combine(a, b) shown\nc3 = combine(1, 2, 3)   # arity 3 → only combine(a, b, c) shown\ncs = combine(args...)   # unknown arity → both signature headers shown","title":"Aggregated docstrings"},{"category":"section","location":"references/#Long-signatures","page":"Reference links & tooltips","text":"Tooltips render multi-line signature headers verbatim and size to fit:\n\np = process(rand(3))               # arity prunes to the vector method\nP = process(rand(3, 3), ones(3))   # arity 2 → the multi-line matrix signature\nt = transform([1, 2, 3], sqrt; rev = true)","title":"Long signatures"},{"category":"section","location":"references/#Configuration","page":"Reference links & tooltips","text":"reference_links = false turns linking off entirely,\npopups = false keeps the links but disables the tooltips.\n\nBoth are keyword arguments of CodeBlocks.","title":"Configuration"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"The plugin's public API is a single type, passed to makedocs(plugins = [CodeBlocks()]):","title":"API reference"},{"category":"section","location":"api/#Demo-API","page":"API reference","text":"The functions below are not part of the package — they exist only while this documentation builds (evaluated into the module by docs/make.jl) so that the manual can demonstrate reference links and tooltips against real docstrings. They also show off how docstrings themselves are rendered: signature headers are highlighted but not numbered or linked, while example blocks inside docstrings get the full treatment.","title":"Demo API"},{"category":"type","location":"api/#DocumenterCodeBlocks.CodeBlocks","page":"API reference","text":"CodeBlocks(; languages=[\"julia\"], reference_links=true, popups=true, line_numbers=true, repl_line_numbers=true, min_lines=1, line_counter=:restart)\n\nDocumenter plugin that enhances code blocks with syntax highlighting, line numbers / linkable lines, and reference links — all in a single pass. Julia is highlighted locally with JuliaSyntax (including julia-repl/jldoctest blocks), so no node/prerender is needed.\n\nlanguages: which code-block languages to process.\nreference_links: wrap identifiers that name a documented object in a link to its docstring. Macro names link too. Names in binding positions — left of =, function parameters, loop variables, … — are not uses and never link.\npopups: doxygen-style hover tooltips on reference links, embedded per page (no fetching): the target's signature and the docstring's first sentence. When a reference matches several documented methods (a bare identifier, a splatted call, …) the tooltip instead lists all candidate signatures to pick from. Requires reference_links=true.\nline_numbers: add the line-number gutter + linkable lines. When false, blocks are still highlighted (and linked) but rendered without a gutter.\nrepl_line_numbers: also add the gutter to REPL transcripts (julia-repl blocks and REPL-style jldoctests). Requires line_numbers=true.\nmin_lines: blocks with fewer lines get an id + permalink but no gutter (the default numbers every block, including one-liners).\nline_counter: the default line-counter mode for every page — :restart, :continue, or :named (see the @codeblocks block below, which overrides it per page, positionally).\n\nReference resolution follows the page's @meta CurrentModule positionally, exactly like @ref: the module in effect at a block's position applies to that block, and code blocks inside a docstring resolve in the docstring's own module.\n\nPages can configure the plugin locally with an @codeblocks block:\n\n```@codeblocks\nline_counter = :continue\n```\n\nline_counter: :restart (default) numbers every block from 1; :continue carries one running line counter across the page's code blocks (julia, julia-repl, executed @repl), tutorial-style; :named keeps one counter per named series — blocks sharing a name (@example name, @repl name, jldoctest name, or a second fence token like `julia name) continue each other, while unnamed blocks restart. Like @meta, the setting applies from its position to the end of the page (or the next @codeblocks block). Blocks inside docstrings are their own page: they always start at 1 and do not advance any counter.\n\nThe first code block of a docstring — the signature header — gets no line numbers or permalink (headers aren't always valid Julia, e.g. f(x[, y])), but the argument and return types in it link to their docstrings; the documented name itself and the parameter names stay plain. Throughout a docstring, references with the enclosing docstring among their candidate targets are not linked — the reader is already there — while calls whose arity resolves to a different documented method still link.\n\nBuild warnings prefixed CodeBlocks: nudge toward tooltip-friendly (and generally better) docstrings: they fire — once per problem, and only for docstrings some code block references — when a docstring lacks a leading signature block or a prose first sentence, when the first sentence exceeds 200 characters, and when a call's arity matches no documented method.\n\nPass to makedocs(plugins=[CodeBlocks()]).\n\n\n\n\n\n","title":"DocumenterCodeBlocks.CodeBlocks"},{"category":"function","location":"api/#DocumenterCodeBlocks.greet","page":"API reference","text":"greet()\n\nPrint a friendly greeting to standard output. A minimal documented function for exercising reference links from code blocks.\n\nExamples\n\nA julia-repl block inside a docstring:\n\njulia> greet()\nHello World!\n\nA script-style jldoctest whose output is NOT valid Julia (and contains the documented name foo) — to see how the highlighter copes:\n\nprintln(\"foo bar baz\")\n\n# output\n\nfoo bar baz\n\n\n\n\n\n","title":"DocumenterCodeBlocks.greet"},{"category":"function","location":"api/#DocumenterCodeBlocks.add_numbers","page":"API reference","text":"add_numbers(a, b)\n\nReturn the sum of a and b. A trivial documented function used in the docs testbed to verify that identifiers in code blocks link to their docstrings.\n\nExamples\n\nA plain julia block (gets highlighting, line numbers, and reference links — note foo links to its docstring):\n\nx = add_numbers(1, 2)\ny = add_numbers(x, foo(10))\n\nA jldoctest block (executed and checked by Documenter):\n\njulia> add_numbers(2, 3)\n5\n\njulia> add_numbers(add_numbers(1, 2), 3)\n6\n\nA julia-repl block with a multi-line input (the whole function … end definition is one input, highlighted together):\n\njulia> function describe(a, b)\n           s = add_numbers(a, b)\n           return \"sum is $s\"\n       end\ndescribe (generic function with 1 method)\n\njulia> describe(2, 3)\n\"sum is 5\"\n\n\n\n\n\n","title":"DocumenterCodeBlocks.add_numbers"},{"category":"type","location":"api/#DocumenterCodeBlocks.MyType","page":"API reference","text":"MyType\n\nA documented type used to check that type identifiers in code blocks resolve to their docstring anchors.\n\nExamples\n\njulia> MyType(3)\nMyType(3)\n\njulia> MyType(3).x\n3\n\n\n\n\n\n","title":"DocumenterCodeBlocks.MyType"},{"category":"method","location":"api/#DocumenterCodeBlocks.foo-Tuple{Any}","page":"API reference","text":"foo(a)\n\nThe one-argument method of foo. Has its own docstring, separate from foo(a, b), to test how links resolve for functions with multiple documented methods.\n\nExamples\n\nA script-style jldoctest (code, then # output, then the expected result):\n\nvalues = [foo(i) for i in 1:3]\ntotal = add_numbers(values[1], values[3])\n\n# output\n\n4\n\n\n\n\n\n","title":"DocumenterCodeBlocks.foo"},{"category":"method","location":"api/#DocumenterCodeBlocks.foo-Tuple{Any, Any}","page":"API reference","text":"foo(a, b)\n\nThe two-argument method of foo, with its own docstring separate from foo(a).\n\nExamples\n\njulia> foo(10, 20)\n30\n\njulia> foo(1)\n1\n\nThe two-argument call above is a self reference (not linked); the one-argument call resolves to the other method's docstring and links.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.foo"},{"category":"method","location":"api/#DocumenterCodeBlocks.qux-Tuple{Int64}","page":"API reference","text":"qux(x::Int)\n\nDouble an integer. Same-arity sibling of qux(x::String) — the two methods differ only in argument type, which a call site like qux(1) cannot disambiguate (we only extract arity), so references to qux list both.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.qux"},{"category":"method","location":"api/#DocumenterCodeBlocks.qux-Tuple{String}","page":"API reference","text":"qux(x::String)\n\nRepeat a string. See qux(x::Int) for the same-arity integer method.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.qux"},{"category":"function","location":"api/#DocumenterCodeBlocks.transform","page":"API reference","text":"transform(v::AbstractVector{T}, f::Function = identity; rev::Bool = false) where {T}\n\nApply f to each element of v, optionally reversing the result. The signature exercises type annotations, a parametric container, a default argument, a keyword argument, and a where clause in the hover tooltip.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.transform"},{"category":"method","location":"api/#DocumenterCodeBlocks.measure-Tuple{Int64}","page":"API reference","text":"measure(x::Int)\n\nMeasure an integer. One of six documented methods of measure, together stress-testing long disambiguation lists.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.measure"},{"category":"method","location":"api/#DocumenterCodeBlocks.measure-Tuple{Float64}","page":"API reference","text":"measure(x::Float64)\n\nMeasure a float, rounding to the nearest integer.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.measure"},{"category":"method","location":"api/#DocumenterCodeBlocks.measure-Tuple{String}","page":"API reference","text":"measure(x::String)\n\nMeasure a string by its length.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.measure"},{"category":"method","location":"api/#DocumenterCodeBlocks.measure-Tuple{Any, Any}","page":"API reference","text":"measure(x, y)\n\nMeasure two things together.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.measure"},{"category":"method","location":"api/#DocumenterCodeBlocks.measure-Tuple{Any, Any, Any}","page":"API reference","text":"measure(x, y, z; scale::Real = 1)\n\nMeasure three things, scaled.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.measure"},{"category":"method","location":"api/#DocumenterCodeBlocks.measure-Tuple{}","page":"API reference","text":"measure()\n\nMeasure nothing at all. The 0-argument method: excluded from disambiguation lists whenever the call site guarantees at least one positional argument, even via a splat like measure(x, rest...).\n\n\n\n\n\n","title":"DocumenterCodeBlocks.measure"},{"category":"function","location":"api/#DocumenterCodeBlocks.combine","page":"API reference","text":"combine(a, b)\n\nCombine two things. combine is included in the docs with a bare @docs DocumenterCodeBlocks.combine entry (no signature), so this docstring and the three-argument one render aggregated inside one docstring <details> — but each docstring still gets its own <section><div>, so both signature headers receive the header treatment (highlight only).\n\n\n\n\n\ncombine(a, b, c)\n\nCombine three things. The second docstring of the aggregated combine entry; its signature header must be stripped of gutter/links just like the first.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.combine"},{"category":"method","location":"api/#DocumenterCodeBlocks.process-Tuple{AbstractMatrix, AbstractVector}","page":"API reference","text":"process(data::AbstractMatrix{<:Real}, weights::AbstractVector{<:Real};\n        normalize::Bool = true, atol::Real = 1e-8,\n        callback::Union{Function, Nothing} = nothing) -> AbstractMatrix\n\nProcess a data matrix with per-column weights. The signature block spans several lines, stress-testing the tooltip layout for long headers.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.process"},{"category":"method","location":"api/#DocumenterCodeBlocks.process-Tuple{AbstractVector}","page":"API reference","text":"process(data::AbstractVector{<:Real}; normalize::Bool = true, atol::Real = 1e-8)\n\nProcess a single data vector. Long single-line signature header, and the same-name sibling of the matrix method above.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.process"},{"category":"function","location":"api/#DocumenterCodeBlocks.clone","page":"API reference","text":"clone(m::MyType; deep::Bool = false) -> MyType\n\nReturn a copy of m. The signature header above references the documented type MyType twice — as the argument annotation and as the -> return type — and both mentions link, while the parameter names and clone itself stay plain.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.clone"},{"category":"macro","location":"api/#DocumenterCodeBlocks.@twice","page":"API reference","text":"@twice(expr)\n\nEvaluate expr twice and return the value of the second evaluation. A documented macro: macro names in code blocks link to their docstring.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.@twice"},{"category":"macro","location":"api/#DocumenterCodeBlocks.@w_str","page":"API reference","text":"w\"text\"\n\nReturn text unchanged. A documented string macro: the w prefix of a w\"…\" literal links to this docstring.\n\n\n\n\n\n","title":"DocumenterCodeBlocks.@w_str"},{"category":"section","location":"linenumbers/#linenumbers","page":"Line numbers","text":"Every processed code block gets a line-number gutter and a stable id, GitHub style. Try it on the block below:\n\nclick a line number to select that line,\nshift-click (or drag along the gutter) to select a range,\nclick the link button (top right of the block) to copy the link — to the current line selection if you have one in that block, else to the whole block; the icon flashes a checkmark to confirm,\nclick the copy button next to it to copy the code — likewise just the selected lines if you have a selection in that block, else the whole block,\nclick anywhere outside the block to deselect.\n\nThe selection is reflected in the URL fragment (#c-1a2b3c4d-L3-L7), so a copied link takes a reader to exactly these lines. The link button is a real anchor, so the browser's usual affordances (right-click → Copy Link, middle-click → new tab) work too. Block ids are content-addressed: they only change when the code itself changes, not when the block moves around on the page.\n\nstruct Point{T <: Real}\n    x::T\n    y::T\nend\n\nnorm2(p::Point) = p.x^2 + p.y^2\n\nfunction closest(points::AbstractVector{<:Point}, target::Point)\n    _, i = findmin(p -> norm2(Point(p.x - target.x, p.y - target.y)), points)\n    return points[i]\nend\n\nLine numbers live in the gutter's CSS, not in the text — selecting and copying code never picks up the digits, and horizontal scrolling keeps the gutter pinned.","title":"Line numbers and linkable lines"},{"category":"section","location":"linenumbers/#REPL-transcripts","page":"Line numbers","text":"Transcripts are numbered too (all lines count, including output), which makes REPL sessions linkable line by line:\n\njulia> p = Point(3.0, 4.0)\nPoint{Float64}(3.0, 4.0)\n\njulia> norm2(p)\n25.0","title":"REPL transcripts"},{"category":"section","location":"linenumbers/#Continued-numbering","page":"Line numbers","text":"By default every block numbers from 1. A page can instead carry one running line counter across its code blocks — tutorial style — by placing an @codeblocks options block:\n\n```@codeblocks\nline_counter = :continue\n```\n\nLike @meta, the setting applies from its position to the end of the page, or until another @codeblocks block flips it back to :restart (the default). All processed blocks participate — julia, julia-repl, and executed @repl blocks — and line permalinks use the displayed numbers, so a copied #…-L12 link keeps meaning the line the reader saw. The two blocks below share one counter:\n\ngrid = [Point(float(i), float(j)) for i in 1:3, j in 1:3]\norigin = Point(0.0, 0.0)\n\nnearest = closest(vec(grid), origin)\nr2 = norm2(nearest)\n\nA third mode, line_counter = :named, keeps one counter per named series: blocks that share a name — @example tutorial, @repl tutorial, jldoctest tutorial, or a plain fence with a second token like julia tutorial — continue each other (across block kinds and across unrelated blocks in between), while unnamed blocks restart. This pairs naturally with Documenter's named @example/@repl sandboxes, where same-named blocks already share one session:\n\ntotal = 1 + 2\nnothing # hide\n\nAn unnamed block between the two restarts at 1, but the series picks up where it left off:\n\ntotal += 3\nnothing # hide\n\nBlocks inside docstrings are their own page: they always start at 1 and never advance any counter.","title":"Continued numbering"},{"category":"section","location":"linenumbers/#Configuration","page":"Line numbers","text":"All knobs are keyword arguments of CodeBlocks:\n\nline_numbers = false disables the gutter (blocks are still highlighted and linked, and keep their id + permalink),\nrepl_line_numbers = false disables the gutter for julia-repl blocks only,\nline_counter sets the site-wide default line-counter mode (:restart, :continue, or :named) — @codeblocks blocks override it per page, positionally,\nmin_lines sets the minimum block length that gets a gutter — the default 1 numbers everything, including one-liners:\n\nanswer = add_numbers(40, 2)\n\nThe signature header of a rendered docstring is deliberately not numbered — it is a header, not example code (its argument and return types do get reference links, though — see Reference links).","title":"Configuration"},{"category":"section","location":"warnings/#warnings","page":"Docstring warnings","text":"The tooltips extract two things from every referenced docstring: the signature (the docstring's leading code block, the Julia convention) and a brief (the first sentence of the first paragraph). When a docstring doesn't provide them, the tooltip degrades gracefully — and the build emits a warning telling the author what to fix. The aim is to nudge docstrings toward a shape that works well in tooltips and reads well everywhere else, too.\n\nThe warnings are prefixed CodeBlocks:, fire once per problem per build, and only for docstrings that some code block actually references.","title":"Docstring warnings"},{"category":"section","location":"warnings/#No-leading-signature-block","page":"Docstring warnings","text":"The tooltip falls back to a signature synthesized from the method object:\n\n┌ Warning: CodeBlocks: the docstring for `MyPackage.frob (Tuple{Int64})` does not start\n│ with a signature code block; tooltips fall back to the synthesized signature\n│ `frob(::Int64)`. Start the docstring with the indented signature (the Julia convention):\n│     \"\"\"\n│         frob(::Int64)\n│\n│     ...\n│     \"\"\"","title":"No leading signature block"},{"category":"section","location":"warnings/#No-prose-paragraph","page":"Docstring warnings","text":"The tooltip shows only the signature:\n\n┌ Warning: CodeBlocks: the docstring for `MyPackage.frob` has no prose paragraph;\n│ tooltips show only the signature. Add a short first sentence describing what it does.","title":"No prose paragraph"},{"category":"section","location":"warnings/#Overlong-first-sentence","page":"Docstring warnings","text":"The brief is clipped at 200 characters if no sentence boundary is found:\n\n┌ Warning: CodeBlocks: the first paragraph of the docstring for `MyPackage.frob` has no\n│ sentence boundary within 200 characters; the tooltip brief is truncated mid-sentence.\n│ Start the docstring with one short summary sentence.","title":"Overlong first sentence"},{"category":"section","location":"warnings/#Arity-without-a-documented-method","page":"Docstring warnings","text":"A code block calls a documented function with an argument count no documented method accepts — often a sign that a method is missing its docstring, or that the example is stale:\n\n┌ Warning: CodeBlocks: no documented method of `MyPackage.frob` takes 4 arguments, but a\n│ code block calls it that way; the tooltip lists all documented methods. Is a method\n│ missing its docstring?\n\n(Documenter itself resolves the equivalent [frob(a, b, c, d)](@ref) silently to the first documented method — the plugin links the same way, it just tells you about it.)","title":"Arity without a documented method"},{"category":"section","location":"highlighting/#highlighting","page":"Syntax highlighting","text":"Julia code is highlighted at build time by parsing each block with JuliaSyntax.jl — the same parser Julia itself uses. Pages ship with the highlighting baked into the HTML, so they render fully styled immediately, and classification always agrees with the actual language grammar.\n\nA plain julia block:\n\nfunction mean_positive(xs::AbstractVector{<:Real})\n    total, n = 0.0, 0\n    for x in xs\n        x > 0 || continue\n        total += x\n        n += 1\n    end\n    return n == 0 ? nothing : total / n\nend\n\nBecause classification comes from a real parse tree, context-sensitive cases come out right: callees get the function-call color, the right-hand side of :: and curly parameters get the type color, :answer is a symbol while quote blocks are not, and string interiors, escapes, and multi-line constructs never confuse the tokenizer:\n\nconfig = Dict(\n    :tolerance => 1e-8,\n    :message => \"multi-line strings?\\nNo problem — even with \\\"quotes\\\".\",\n)\nT = eltype(Vector{Float64})","title":"Syntax highlighting"},{"category":"section","location":"highlighting/#REPL-transcripts","page":"Syntax highlighting","text":"julia-repl blocks are highlighted too: prompts are marked, the input expressions get full highlighting (and reference links), and output is left plain. Executed @repl blocks are rebuilt into the same transcript form (their ANSI-colored output is kept as-is). Multi-line input is handled by parsing until the expression is complete:\n\njulia> function double(x)\n           2x\n       end\ndouble (generic function with 1 method)\n\njulia> double(21)\n42","title":"REPL transcripts"},{"category":"section","location":"highlighting/#Doctests","page":"Syntax highlighting","text":"jldoctest blocks work the same way. Script-style doctests split on the # output marker exactly like Documenter does — the input is highlighted, the output is not:\n\nx = add_numbers(1, 2)\nprintln(\"x = \", x)\n\n# output\n\nx = 3\n\nBlocks in other languages (and @example output, nohighlight, …) are left untouched. The languages option of CodeBlocks controls which block languages are processed.","title":"Doctests"},{"category":"section","location":"highlighting/#Parse-tolerance","page":"Syntax highlighting","text":"Highlighting is best-effort on invalid code: JuliaSyntax parses with error recovery, so partially-broken snippets (work-in-progress examples, docstring-style pseudo-signatures like f(x[, y])) still get sensible colors instead of falling back to plain text.","title":"Parse tolerance"},{"category":"section","location":"#DocumenterCodeBlocks.jl","page":"Home","text":"A Documenter.jl plugin that enhances Julia code blocks:\n\nLine numbers and linkable lines, GitHub style: every block gets a permalink, and clicking the gutter selects lines and puts a stable fragment like #c-1a2b3c4d-L3-L7 in the URL.\nReference links: identifiers in code blocks that name a documented object link to its docstring — call-arity aware, so foo(1, 2) links to the foo(a, b) method documentation. Macro names link too.\nHover tooltips on every reference link, doxygen style: the target's signature and a one-line summary, embedded at build time. Ambiguous references show the candidate-method list.\nSyntax highlighting of Julia code blocks at build time with JuliaSyntax.jl. Works for julia, julia-repl, jldoctest, and executed @repl blocks alike.\nDocstring-quality warnings that nudge toward tooltip-friendly docstrings.\n\nEvery code block in this manual is rendered by the plugin — hover, click, and select away.","title":"DocumenterCodeBlocks.jl"},{"category":"section","location":"#Installation","page":"Home","text":"Install the package from the General registry:\n\njulia> import Pkg\n\njulia> Pkg.add(\"DocumenterCodeBlocks\")","title":"Installation"},{"category":"section","location":"#Usage","page":"Home","text":"Add the plugin to your docs/Project.toml and pass it to makedocs:\n\nusing Documenter, DocumenterCodeBlocks\n\nmakedocs(\n    sitename = \"MyPackage\",\n    # ...\n    plugins = [CodeBlocks()],\n)\n\nThat is all — the CSS/JS assets are injected automatically and the default Documenter.HTML format needs no extra options. See CodeBlocks for configuration.\n\nFor unqualified names in code blocks to resolve to docstrings, set the page's CurrentModule — exactly as for @ref links:\n\n```@meta\nCurrentModule = MyPackage\n```","title":"Usage"},{"category":"section","location":"#Status","page":"Home","text":"Experimental. The plugin builds on Documenter internals beyond the documented plugin API, so the supported Documenter version is pinned tightly in the package's compat; expect breakage on untested Documenter releases.","title":"Status"}]
}
